version: '3.8'

services:
  app:
    image: wordly-spring:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "9080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
